# Heart-Healthy Dietary Application

This app was developed as a practical project designed to help older adult users track and follow a heart-healthy diet plan. 

## 🔒 Regulatory & Security Notice 
This application is **not deployed live in production**. Because software providing dietary guidance for cardiovascular health can intersect with software-as-a-medical-device (SaMD) qualifications, it is kept in a secure, isolated development environment for compliance and safety reasons. 

However, the codebase is **ready to run locally** to demonstrate and test the prototype.

---

## 🚀 How to Run the App (via GitHub Codespaces)

You can launch and run the app directly in your browser using **GitHub Codespaces**.

### Step-by-Step Instructions:

1. **Launch the Codespace:**
   * At the top of this GitHub repository page, click the green **"Code"** button.
   * Select the **"Codespaces"** tab.
   * Click **"Create codespace on main"**. (This opens a virtual development environment in a new browser tab).

2. **Wait for Initialization:**
   * Wait a few moments for the virtual container to load and set up the dependencies.

3. **Start the Application:**
   * In the terminal at the bottom of the screen, type the following command and press `Enter`:
     ```bash
     npm run dev
     ```

4. **Open the App:**
   * Click the **"Open in Browser"** button in the notification that appears in the bottom-right corner.
   * The interactive app will open in a new tab!

---

## 🛠️ Tech Stack & Features
* **Core Logic:** Built with [T.ex. Python / JavaScript] to structure dietary metrics and user inputs.
* **Architecture:** Designed with clean separation of concerns, suitable for future regulatory documentation audits.
